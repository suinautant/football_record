<!DOCTYPE html>
<html lang="ko">
  <head>
    <%- include('common/__head'); -%>
  </head>
  <body>
    <%- include('common/__menu'); -%>
    <div id="jsRoot" class="container">
      <div>
        <div class="bg-light rounded">
          <div class="p-3 py-4">
            <h1 class="pb-4">역대 전적</h1>
            <table class="table table-striped table-hover">
              <thead>
                <tr>
                  <th scope="col" class="text-center">#</th>
                  <th scope="col" class="text-center">대회</th>
                  <th scope="col" class="text-center">라운드</th>
                  <th scope="col" class="text-center">상대팀</th>
                  <th scope="col" class="text-center">날짜</th>
                  <th scope="col" class="text-center">장소</th>
                  <th scope="col" class="text-center">득점</th>
                  <th scope="col" class="text-center">실점</th>
                </tr>
              </thead>
              <tbody id="jsTable"></tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
    <%- include('common/__body_footer'); -%>
    <script src="/javascripts/middleware/xhr.js" type="text/javascript"></script>
    <script>
      async function list() {
        console.log('API SERVER : ' + API_SERVER_BASE);
        const req = new XHR('http://localhost:3000/api');
        // const getRes = await req.get('/match');
        // const res = await req.get('/match');
        req
          .get('/match')
          .then((data) => {
            console.log('DATA');
            console.log(data);
          })
          .catch((error) => {
            console.log('ERROR');
            console.log(error);
          });
      }
      list();
    </script>
  </body>
</html>
